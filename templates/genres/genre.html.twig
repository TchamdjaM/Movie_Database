{% extends 'base.html.twig' %}

{% block title %}Un Genre{% endblock %}
{% block pageTitle %}Un Genre{% endblock %}

{% block body %}

<h1 style="text-align: center;color: blue;">Fiche d'un genre </h1> 

<table class="table table-bordered table-condensed">
    <thead>
        <tr>
            <th>Nom</th>
            <th>Film</th>
        </tr>
    </thead>
    <tbody>
    
        <tr>
            <td>{{ genres.nom }}</td>

            <td>
            /*<a href="{{ url('app_films_film', {'id': film.id}) }}">{{ films.titre }}</a>*/
            </td>
        </tr>
    </tbody>
</table>

{% endblock %}

